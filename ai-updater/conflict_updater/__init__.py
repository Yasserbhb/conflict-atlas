"""Conflict Atlas AI updater — an agent pipeline that scans a period and proposes
sourced events/conflicts to feed into the app's seed.json.

Public entrypoint: `conflict_updater.pipeline.scan`.
"""
__version__ = "0.1.0"
