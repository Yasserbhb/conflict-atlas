# Findings log

Plain-English weekly digests written by the automated pipeline (`conflict_updater auto`).
Each file is one run: what was **added** to the atlas, what was **held** (found but too
uncertain to auto-add), and what was **already known**. This is the thing to read — nothing here
needs any action.
